[pytest]
testpaths = tests
addopts = -v --tb=short --strict-markers
markers =
    integration: end-to-end pipeline tests (slow)
    asyncio: mark test as async
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::FutureWarning
